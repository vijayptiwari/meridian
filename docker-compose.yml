services:
  meridian:
    build: .
    ports:
      - "3030:3030"
    volumes:
      - ./data:/app/data
      - ./src/config.json:/app/src/config.json
      - ./resume:/app/resume
    env_file:
      - .env
    environment:
      JOB_AGENT_UI_HOST: "0.0.0.0"
